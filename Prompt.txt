スマホ版（のみ）の battle 画面を再修正してください。PC版は絶対に変更しないでください。
出力は diff 形式、不要なリファクタ禁止、最小差分で。

【要件1】スマホ：中央フィールドが「上側」で欠けている（石投下エリア〜盤面上端が見切れている）
現状、前回より少し改善したが依然として中央フィールド全体が画面内に収まっていません。
特に「上側」が欠けており、石投下エリア（上の落下領域）と盤面上端が見切れています。
PC版（正常）と同様に、スマホでも中央フィールドが欠けずに全て表示されるようにしてください。

さらに、左右キャラエリアと中央フィールドの間に不要な隙間が残っています。
中央フィールドが全て映ることを最優先にしたうえで、
左右との隙間ができるだけ出ないように中央フィールドを大きく表示してください。

※「画面内に全て収まる」の定義：
- 石投下エリア（上の落下領域）が欠けない
- 盤面（グリッド）の上端〜下端が欠けない
- 下のゲージ/列選択エリアも欠けない
- スマホで縦スクロールを発生させない

※ヒント（強制ではないが、この方向が安全）：
- transform: scale() を使う場合、スケール対象ラッパを
  position: fixed; inset: 0; overflow: hidden;
  のように「表示領域＝viewport」に固定し、
  その領域内で盤面＋石投下＋下UIが全て入る scale を算出してください。
- 盤面が上に寄りすぎないように、縦方向のセンタリング（translateY / flex align / top計算）も調整してください。

【要件2】スマホ：左右キャラエリアの AbilityDetail_2 が表示されていない
左右キャラエリアの必殺技効果欄（AbilityDetail_2）がスマホで消えています。
PCと同様に表示してください（スマホでも非表示にしない）。

制約：
- 変更はスマホのみ（メディアクエリ等で分岐）
- PCには一切影響を出さない
- 最小差分で修正
